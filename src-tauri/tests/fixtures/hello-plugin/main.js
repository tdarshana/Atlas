// The hello-world plugin's entry point. Task 3 wires this into the web host; for now it
// only proves that `plugin_read_main` returns the right file.
console.log("hello from the hello-world plugin");
