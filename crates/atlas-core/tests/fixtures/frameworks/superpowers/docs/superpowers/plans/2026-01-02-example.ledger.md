# Example fixture ledger

A plan-directory ledger file, named with the `.ledger.md` convention rather than
living under `.superpowers/sdd/*/progress.md`. `documents()` must classify it as
`FrameworkDocType::Ledger`, not `Plan`, even though it sits in `PLANS_DIR`.

Ruling: this is a ledger, not a plan.
