# Example fixture plan two

A second plan file, so a test can check that two files sharing a heading's exact
text get their own, separate parent tasks instead of colliding on it.

### Task 1: Set up the widget

- [ ] **Step 1: Write the other widget module**
