{
  "mcpServers": {
    "packaged": {
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/server",
      "args": ["--serve"],
      "env": {
        "PLUGIN_TOKEN": "SECRET-DO-NOT-LEAK"
      }
    },
    "packaged-off": {
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/other",
      "args": []
    }
  }
}
