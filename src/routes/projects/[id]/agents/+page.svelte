<script lang="ts">
	// The Agents tab. The hub layout owns the header and the tabs; this body arrives with
	// the rest of phase 8.
	import EmptyState from '$lib/ui/EmptyState.svelte';
</script>

<EmptyState title="Agents" hint="Project agents are exported into this root only. Global agents still apply." />
