<script lang="ts">
	// The Practices tab. The hub layout owns the header and the tabs; this body arrives with
	// the rest of phase 8.
	import EmptyState from '$lib/ui/EmptyState.svelte';
</script>

<EmptyState title="Practices" hint="A practice is a standing rule agents should follow, written in Markdown." />
