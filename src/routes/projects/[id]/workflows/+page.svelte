<script lang="ts">
	// The Workflows tab. The hub layout owns the header and the tabs; this body arrives with
	// the rest of phase 8.
	import EmptyState from '$lib/ui/EmptyState.svelte';
</script>

<EmptyState title="Workflows" hint="A workflow runs on a schedule or a prompt, and its recent runs are listed here." />
