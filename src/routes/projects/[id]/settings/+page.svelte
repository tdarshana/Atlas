<script lang="ts">
	// The Project settings tab. The hub layout owns the header and the tabs; this body arrives with
	// the rest of phase 8.
	import EmptyState from '$lib/ui/EmptyState.svelte';
</script>

<EmptyState title="Project settings" hint="Name, key prefix, agent access and the extraction override for this project." />
